1. @是不顯示該行的指令，通常會在批次檔的第一行就直接用，省得每一行指令都要寫一次。
指令:
@echo off

2. ::跟REM是一樣的功能，就是作為註解說明用途。
指令:
:: 我是註解
REM 這行也是註解

3. %之後會接著一個數字，用來接收傳進來的參數。
%0代表批次檔本身，%1代表第一個參數，其餘依此類推，最多可以有255個。
指令:
@echo off
echo 批次檔名是：%0
echo %1！%2

執行:
test 您好 alexc

結果:
批次檔名是：test
您好！alexc
